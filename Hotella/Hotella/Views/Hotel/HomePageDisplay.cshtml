@using Hotella.ViewModels
@model HotelsViewModel

@{
    ViewData["Title"] = "Home";
}

<header class="bg-primary text-white text-left py-5">
    <div class="container">
        <h1>Forget Busy Work, Start Your Next Vacation</h1>
        <p class="lead">Explore the best hotels for your next getaway</p>
        <a href="@Url.Action("HotelsDisplay", "Hotel")" class="btn btn-light">SHOW ME NOW</a>


    </div>
</header>

<!-- Most Picked Hotels Section -->
<section class="container my-5">
    <h2 class="text-left mb-4">Most Picked</h2>
    <div class="row">
        @foreach (var hotel in Model.MostPickedHotels)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow">
                    <img src="@hotel.ImageUrl" class="card-img-top" alt="@hotel.Name" />
                    <div class="card-body">
                        <h5 class="card-title">@hotel.Name</h5>
                        <p class="card-text">@hotel.City</p>
                        <a href="@Url.Action("HotelDetails", "Hotel", new { hotelName = hotel.Name })" class="btn btn-primary">BOOK NOW</a>

                    </div>
                </div>
            </div>
        }
        
    </div>
</section>

<!-- Most Picked Hotels Section -->
<section class="container my-5">
    <h2 class="text-left mb-4">Houses With Beautiful Backyard</h2>
    <div class="row">
        @foreach (var hotel in Model.HotelsWithBackyard)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow">
                    <img src="@hotel.ImageUrl" class="card-img-top" alt="@hotel.Name" />
                    <div class="card-body">
                        <h5 class="card-title">@hotel.Name</h5>
                        <p class="card-text">@hotel.City</p>
                        <a href="@Url.Action("HotelDetails", "Hotel", new { hotelName = hotel.Name })" class="btn btn-primary">BOOK NOW</a>

                    </div>
                </div>
            </div>
        }

    </div>
</section>

<!-- Hotels with swimming pool section -->
<section class="container my-5">
    <h2 class="text-left mb-4">Hotels with swimming pools</h2>
    <div class="row">
        @foreach (var hotel in Model.HotelsWithSwimmingPool)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow">
                    <img src="@hotel.ImageUrl" class="card-img-top" alt="@hotel.Name" />
                    <div class="card-body">
                        <h5 class="card-title">@hotel.Name</h5>
                        <p class="card-text">@hotel.City</p>
                        <a href="@Url.Action("HotelDetails", "Hotel", new { hotelName = hotel.Name })" class="btn btn-primary">BOOK NOW</a>

                    </div>
                </div>
            </div>
        }

    </div>
</section>

<!-- Hotels with a kitchen set Section -->
<section class="container my-5">
    <h2 class="text-left mb-4">Apartments with kitchen set</h2>
    <div class="row">
        @foreach (var hotel in Model.HotelsWithKitchen)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow">
                    <img src="@Url.Content(hotel.ImageUrl)" class="card-img-top" alt="@hotel.Name" />
                    <div class="card-body">
                        <h5 class="card-title">@hotel.Name</h5>
                        <p class="card-text">@hotel.City</p>
                        <a href="@Url.Action("HotelDetails", "Hotel", new { hotelName = hotel.Name })" class="btn btn-primary">BOOK NOW</a>

                    </div>
                </div>
            </div>
        }

    </div>
</section>

@* <!-- Testimonials Section -->
<section class="container my-5">
    <h2 class="text-center">Happy Customers</h2>
    <div id="testimonialCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            @foreach (var testimonial in Model.Testimonials.Select((value, index) => new { value, index }))
            {
                <div class="carousel-item @(testimonial.index == 0 ? "active" : "")">
                    <div class="text-center">
                        <img src="@testimonial.value.ImageUrl" class="rounded-circle mb-3" width="100" height="100" alt="@testimonial.value.CustomerName" />
                        <p class="fw-bold">@testimonial.value.CustomerName</p>
                        <p>"@testimonial.value.Review"</p>
                        <p>⭐⭐⭐⭐⭐</p>
                    </div>
                </div>
            }
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#testimonialCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#testimonialCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>
    </div>
</section>

@section Scripts {
    <script>
        var myCarousel = new bootstrap.Carousel(document.querySelector('#testimonialCarousel'), {
            interval: 3000
        });
    </script> *@

